(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main (params) (returns)
            (locals
                (Array I64)
            )

            (Dump (local 0))

            (Move (array I64 1 2 3) (local 0))
            (Dump (local 0))

            (Move (array I64 4 5 6) (local 0))
            (Dump (local 0))

            (Move (array I64) (local 0))
            (Dump (local 0))

            (Move 9 $stack)
            (Move 7 $stack)
            (Dump (array I64 $stack 8 $stack))

            (Move (array I64 10) (local 0))
            (GetElement I64 (UI64 0) (local 0) $stack)
            (Dump $stack)

            (Exit 0)
        )
    )
)