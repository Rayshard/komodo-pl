(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main (params) (returns)
            (locals
                I64
                (I64 myLocalI64)
                (Bool myLocalBool)
            )

            (Assert (local 0) 0)
            (Assert (local myLocalI64) 0)
            (Assert (local myLocalBool) false)

            (Move 1 (local 0))
            (Assert (local 0) 1)
            (Move 2 (local 1))
            (Assert (local myLocalI64) 2)
            (Move true (local 2))
            (Assert (local myLocalBool) true)

            (Exit 0)
        )
    )
)