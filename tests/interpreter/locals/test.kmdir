(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main
            (locals
                I64
                (I64 myLocalI64)
                (Bool myLocalBool)
            )

            (Dump (local 0))
            (Dump (local myLocalI64))
            (Dump (local myLocalBool))

            (Store 1 (local 0))
            (Dump (local 0))
            (Store 2 (local 1))
            (Dump (local myLocalI64))
            (Store false (local 2))
            (Dump (local myLocalBool))

            (Exit 0)
        )
    )
)