(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (data
            (myData "abcdef123")
        )

        (function Main (params) (returns)
            (GetLength (data MyModule myData) $stack)
            (Assert (pop UI64) (UI64 9))

            (GetElement UI8 (UI64 2) (data MyModule myData) $stack)
            (Assert (pop UI8) (UI8 99))

            (Exit 0)
        )
    )
)