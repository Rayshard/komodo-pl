(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main
            (args)
            (locals (Array I64))
            (returns)
            (basicBlock __entry__
                (Print (Array I64) $local0)

                (Store ((Array I64) (1 2 3)) $local0)
                (Print (Array I64) $local0)

                (Store (Array I64 4 5 6) $local0)
                (Print (Array I64) $local0)

                (Store (Array I64) $local0)
                (Print (Array I64) $local0)

                (Load 9)
                (Load 7)
                (Print (Array I64) (Array I64 $stack 8 $stack))

                (Store (Array I64 10) $local0)
                (GetElement I64 (UI64 0) $local0 $stack)
                (Print I64 $stack)

                (Load 0)
                (Syscall Exit)
            )
        )
    )
)