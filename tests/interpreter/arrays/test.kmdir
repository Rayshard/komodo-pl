(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main
            (args)
            (locals (Array I64))
            (returns)
            (basicBlock __entry__
                (Print (Array I64) $local0)

                (Store ((Array I64) (1 2 3)) $local0)
                (Print (Array I64) $local0)

                (Store (Array I64 4 5 5) $local0)
                (Print (Array I64) $local0)

                (Store (Array I64) $local0)
                (Print (Array I64) $local0)

                (Load 0)
                (Syscall Exit)
            )
        )
    )
)