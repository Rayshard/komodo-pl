(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main
            (locals
                (Array I64)
            )

            (Print (Array I64) (local 0))

            (Store ((Array I64) (1 2 3)) (local 0))
            (Print (Array I64) (local 0))

            (Store (Array I64 4 5 6) (local 0))
            (Print (Array I64) (local 0))

            (Store (Array I64) (local 0))
            (Print (Array I64) (local 0))

            (Load 9)
            (Load 7)
            (Print (Array I64) (Array I64 $stack 8 $stack))

            (Store (Array I64 10) (local 0))
            (GetElement I64 (UI64 0) (local 0) $stack)
            (Print I64 $stack)

            (Syscall Exit (0))
        )
    )
)