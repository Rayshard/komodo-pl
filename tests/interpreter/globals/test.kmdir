(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (globals
            (I64 myGlobal)
        )

        (function Main (params) (returns)
            (Assert (global MyModule myGlobal) 0)

            (Move 1 (global MyModule myGlobal))
            (Assert (global MyModule myGlobal) 1)

            (Exit 0)
        )
    )
)