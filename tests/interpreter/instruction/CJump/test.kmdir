(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main (params) (returns)
            (CJump badExit false)
            (CJump goodExit true)

            (Exit 2)

          (label badExit)
            (Exit 1)

          (label goodExit)
            (Exit 0)
        )
    )
)