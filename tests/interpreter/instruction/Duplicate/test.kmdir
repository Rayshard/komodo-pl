(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main (params) (returns)
            (PushConstant 123)
            (Duplicate)
            (Assert (pop I64) 123)
            (Assert (pop I64) 123)

            (Exit 0)
        )
    )
)