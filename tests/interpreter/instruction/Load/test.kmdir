(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main
            (Load 11)
            (Assert $stack 11)

            (Call MyModule TestLoadArg 12)
            (Call MyModule TestLoadLocal)

            (Load 14)
            (Load 15)
            (Load $stack)
            (Assert $stack 15)
            (Assert $stack 14)

            (Load 0)
            (Syscall Exit)
        )
        (function TestLoadArg (params I64)
            (Load (arg 0))
            (Assert $stack 12)
            (Return)
        )
        (function TestLoadLocal
            (locals I64)

            (Load (local 0))
            (Assert $stack 0)
            (Return)
        )
    )
)