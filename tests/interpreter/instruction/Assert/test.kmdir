(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main
            (Load true)
            (Assert $stack true)
                
            (Load false)
            (Assert $stack true)

            (Print I64 2)
        )
    )
)