(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main (params) (returns)
            (locals
                (Array I64)
            )
            
            (Move (array I64 123 456 789) (local 0))
            
            (GetElement (UI64 0) (local 0) $stack)
            (Assert (pop I64) 123)

            (GetElement (UI64 1) (local 0) $stack)
            (Assert (pop I64) 456)

            (GetElement (UI64 2) (local 0) $stack)
            (Assert (pop I64) 789)

            (Exit 0)
        )
    )
)