(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main (params) (returns)
            (locals
                (Ref I64)
            )

            (Allocate I64 (local 0))
            (Store 123 (local 0))
            (Load (local 0) $stack)
            (Assert (pop I64) 123)

            (Exit 0)
        )
    )
)