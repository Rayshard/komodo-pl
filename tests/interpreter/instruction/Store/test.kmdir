(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main
            (locals)
            
            (Call MyModule TestStoreLocal)

            (Store 15 $stack)
            (Assert $stack 15)

            (Syscall Exit (0))
        )
        (function TestStoreLocal
            (locals I64)

            (Store 55 (local 0))
            (Assert (local 0) 55)
            (Return)
        )
    )
)