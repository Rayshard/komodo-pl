(program MyProgram
    (entry MyModule Main)
    (module System
        (function Test (params) (returns UI64)
            (locals
                ((Const I64) myLocal)
            )
        )
    )
)