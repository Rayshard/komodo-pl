(program MyProgram
    (entry MyModule Main)
    (module MyModule
        (function Main (params) (returns)
            (locals)
          
          (label start)
            (Print I64 123)

            (Syscall Exit (0))
        )
    )
)